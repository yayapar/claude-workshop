# Edge Cases

Link to https://example.com/docs#installation — the fragment is not a tag.

Issue #123 is purely numeric, so it is not a tag either.

Nested tags work: #project/alpha and #project/beta.

Trailing punctuation is trimmed: #done, #shipped. And #wrapped!

Hyphens and underscores are kept: #in-progress #needs_review
